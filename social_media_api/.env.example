# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=false
# Comma-separated hosts, e.g. example.com,.example.com,api.example.com
DJANGO_ALLOWED_HOSTS=*
# Optional, comma-separated full scheme+host for CSRF (e.g. https://yourapp.herokuapp.com)
DJANGO_CSRF_TRUSTED_ORIGINS=
DJANGO_SECURE_SSL_REDIRECT=true

# Database (default falls back to SQLite if not set)
# For Postgres: postgres://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_URL=
